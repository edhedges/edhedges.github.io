# Netlify headers configuration
# Serve PDFs inline in browser instead of forcing download

/static/*.pdf
  Content-Disposition: inline
  Content-Type: application/pdf

# Enable CORS for all static assets
/static/*
  Access-Control-Allow-Origin: *
